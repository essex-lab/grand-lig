name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - MDAnalysis
  - mdtraj
  - openmm
  - parmed
  - pymbar
  - rdkit
  - scipy
  - tqdm
  - openmmtools

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

