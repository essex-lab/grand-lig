name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - MDAnalysis
  - mdtraj
  - openmm
  - parmed
  - pymbar
  - scipy
  - tqdm
  - openmmtools

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

